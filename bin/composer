#!/bin/sh
exec docker run --rm --network host \
  --user "$(id -u):$(id -g)" \
  -e COMPOSER_HOME=/tmp/composer-home \
  -v /home/piston/.cache/ecommerce-composer:/tmp/composer-home \
  -v "$(pwd)":/app -w /app \
  lunar-starter composer "$@"
